@page
@model TP2.Pages.ListNotesModel
@{
    ViewData["Title"] = "Notas Salvas";
}

<h2>Notas Salvas</h2>

<ul>
    @foreach (var file in Model.FileNames)
    {
        <li>
            <a asp-page="ViewNote" asp-route-file="@file">@file</a>
        </li>
    }
</ul>
